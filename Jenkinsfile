pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello from Jenkinsfile!'
                sh 'ls -la'           // just to see what files are there
            }
        }
    }
}
